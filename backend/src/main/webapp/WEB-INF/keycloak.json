{
  "realm": "${env.KC_REALM_NAME}",
  "auth-server-url": "${env.KC_AUTH_SERVER_URL}",
  "ssl-required": "${env.KC_SSL_REQUIRED:all}",
  "resource": "${env.KC_RESOURCE_NAME}",
  "credentials": {
    "secret": "${env.KC_SECRET}"
  },
  "confidential-port": 443
}
