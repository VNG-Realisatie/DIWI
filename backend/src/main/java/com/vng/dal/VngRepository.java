package com.vng.dal;

import org.hibernate.Session;

public class VngRepository extends AbstractRepository {

    public VngRepository(Session session) {
        super(session);
    }

    //TODO - add methods with DB queries
}
